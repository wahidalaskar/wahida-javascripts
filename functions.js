//connst cashWithdrawal = (amount, accnum) => {
  //  console.log(`Withdrawing ${amount} from account ${ accnum}');const takeOrder = (size, drinkType) => {
  //    console.log(`Order received: ${size} ${drinkType}`);
  // }
    
    let orderCount = 0
    const takeOrder = (topping) => {
     console.log(`Pizza with ${topping}`);
     orderCount++;
    }
    //console.log("pizza",with "hum","pineapple",)
    //takeOrder("pineapple",)
    takeOrder("pizza", "hum","pineapple",) 
    const takeOrder =({'pizza'with ${"pineapple"}),
    const takeOrder =({'pizza'with ${"chicken"}); 
    const takeOrder = ({'pizza'with ${"hum"});
//  console.log(`Pizza with ${topping}`);
     

